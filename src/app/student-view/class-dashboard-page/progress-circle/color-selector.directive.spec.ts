import { ColorSelectorDirective } from './color-selector.directive';

describe('ColorSelectorDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorSelectorDirective();
    expect(directive).toBeTruthy();
  });
});
